## Packages
socket.io-client | Real-time updates for incoming webhook requests
framer-motion | Smooth animations for request list and transitions
date-fns | Formatting timestamps
lucide-react | Icons (already in base, but ensuring usage)
react-syntax-highlighter | Pretty printing JSON bodies and headers
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["'Space Grotesk'", "sans-serif"],
  mono: ["'JetBrains Mono'", "monospace"],
  sans: ["'Inter'", "sans-serif"],
}
